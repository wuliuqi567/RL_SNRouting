
# base config
agent: 'GAT' #
project_name: "SN_GAT"

# environment parameters
n_order_adj: 1 # number of order for adjacency matrix


#gat parameters
num_layers: 1 # corresponding to modifying n_order_adj
num_heads: 2
gat_hidden_feats: 8
mlp_hidden_feats: 32

#gat learner parameters
learning_rate: 0.0005
lr_decay_steps: 100000
